export interface Service{
    titulo: string;
    descripcion: string;
    gradient: string;
}

export interface TeamMember{
    nombre: string;
    puesto: string;
    descripcion: string;
    gradient: string;
}